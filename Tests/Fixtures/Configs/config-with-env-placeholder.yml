output_dir: ${SWIFTGEN_OUTPUT_DIR}
strings:
  inputs: Sources1/Folder
  outputs:
    templateName: structured-swift5
    output: strings.swift
    params:
      foo: 5
      bar:
        bar1: 1
        bar2: 2
        bar3:
          - 3
          - 4
          - bar3a: 50
      baz:
        - ${HELLO}
        - $HELLO world
