input_dir: /input
output_dir: /output
colors:
  inputs: colors/paths
  outputs:
    templatePath: /templates/colors/templates.stencil
    output: colors/out.swift

fonts:
  inputs: fonts/paths
  outputs:
    templatePath: /templates/fonts/templates.stencil
    output: fonts/out.swift

ib:
  inputs: ib/paths
  outputs:
    templatePath: /templates/ib/templates.stencil
    output: ib/out.swift

strings:
  inputs: strings/paths
  outputs:
    templatePath: /templates/strings/templates.stencil
    output: strings/out.swift

xcassets:
  inputs: xcassets/paths
  outputs:
    templatePath: /templates/xcassets/templates.stencil
    output: xcassets/out.swift
